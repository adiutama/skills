# Acknowledge

Post immediately after Slack MCP is resolved — before deep triage.

## Message

```text
:eyes: Looking into this.
```

Keep it one line. No diagnosis yet.

## How to post

1. Resolve Slack MCP per [slack-mcp.md](slack-mcp.md).
2. When `has_thread` is true, post the ack as a **thread reply** via MCP (`channel` + `thread_ts`).
3. Mark tracker **ack** → done.

No thread target → ask for Slack permalink or manual paste; then mark done when posted.
